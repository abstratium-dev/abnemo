import { expect, Page } from '@playwright/test';

// TODO: rename this file

// TODO: Replace with actual element selectors

// TODO: create functions for doing things on pages, e.g.:
/*
export async function addHost(page: Page, hostName: string): Promise<string> {
    console.log(`Adding new host '${hostName}'...`);
    
    // Wait for the page to load and the Add Client button to be visible
    const addHostButton = page.getByRole('button', { name: /Add Host/i });
    await expect(addHostButton).toBeVisible({ timeout: 10000 });
    await addHostButton.click();

    // Fill in the form
    
    // Submit the form
}
 */

